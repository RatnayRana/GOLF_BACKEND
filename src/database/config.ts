const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "admin",
        database: "nodemysql",
    },
    port: 3000,
    listPerPage: 100
};
export default config;